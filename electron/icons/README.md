# App icon

`icon.png`, `icon.icns` and `icon.ico` are static exports of the circular JARVIS SURVIVAL artwork drawn by `electron/jarvis.js` at its idle, reduced-motion frame.

The original app name and artwork are retained. The proposed compass redesign is not used. These project-created assets use the project's MIT license.
